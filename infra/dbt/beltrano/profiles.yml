beltrano_oltp_content:
  target: dev
  outputs:
    dev:
      type: postgres
      host: host.docker.internal
      user: postgres
      password: postgres
      port: 5432
      dbname: beltrano_dw
      schema: stage
      threads: 1